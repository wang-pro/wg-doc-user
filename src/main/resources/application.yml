server:
  port: 9005
  servlet:
    context-path: /doc-user-api

spring:
  application:
    name: doc-user
  profiles:
    active: dev
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: update
logging:
  file: logs.log
